import { createClient } from '@supabase/supabase-js';
const url=String(import.meta.env.VITE_SUPABASE_URL||'').replace(/\/+$/,'').replace(/\/rest\/v1$/i,'');
const key=String(import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY||'').trim();
if(!url||!key)throw new Error('Supabase não configurado.');
const sb=createClient(url,key),ADMIN='willianwca2011@gmail.com';
function addInventoryField(form){if(form.querySelector('[data-inline-accounts]'))return;const title=form.querySelector('h2')?.textContent||'';if(title.toLowerCase().includes('editar'))return;const delivery=form.querySelector('[name="delivery_type"]');if(!delivery)return;const wrap=document.createElement('label');wrap.setAttribute('data-inline-accounts','1');wrap.innerHTML='Estoque inicial de contas <textarea name="inline_accounts" maxlength="50000" placeholder="conta1:senha1\nconta2:senha2\nconta3:senha3"></textarea><small style="display:block;color:#6b7280;margin-top:6px;font-weight:500">Somente para entrega Digital. Uma conta por linha no formato nome:senha. Depois, você poderá repor o estoque pela categoria <b>Estoque de contas</b>.</small>';const status=form.querySelector('[name="active"]')?.closest('label');(status?.parentElement||form).insertBefore(wrap,status||form.querySelector('.actions'));const toggle=()=>{const digital=delivery.value==='digital';wrap.style.display=digital?'block':'none';const ta=wrap.querySelector('textarea');ta.disabled=!digital;if(!digital)ta.value=''};delivery.addEventListener('change',toggle);toggle()}
function parseAccounts(raw){return String(raw||'').split(/\r?\n/).map(x=>x.trim()).filter(Boolean).map(line=>{const i=line.indexOf(':');if(i<=0||i>=line.length-1)throw new Error(`Conta inválida: ${line}. Use nome:senha.`);return{login:line.slice(0,i).trim(),password:line.slice(i+1).trim()}})}
async function isAdmin(){const{data}=await sb.auth.getSession();return String(data.session?.user?.email||'').toLowerCase()===ADMIN}
async function handleSubmit(e,form){const title=form.querySelector('h2')?.textContent||'';if(title.toLowerCase().includes('editar'))return;const delivery=form.querySelector('[name="delivery_type"]')?.value,ta=form.querySelector('[name="inline_accounts"]');if(delivery!=='digital'||!ta||!ta.value.trim())return;e.preventDefault();e.stopImmediatePropagation();if(!(await isAdmin()))return alert('Acesso negado.');let accounts;try{accounts=parseAccounts(ta.value)}catch(err){alert(err.message);return}if(!accounts.length){alert('Adicione pelo menos uma conta.');return}const get=n=>form.querySelector(`[name="${n}"]`)?.value??'';const data={name:String(get('name')).trim(),description:String(get('description')).trim(),price:Number(get('price')),stock_quantity:accounts.length,method:String(get('method')||'digital').trim(),robux:Number(get('robux')||0),category_slug:get('category_slug')||null,image_url:String(get('image_url')).trim()||null,delivery_type:'digital',active:form.querySelector('[name="active"]')?.value==='true',featured:form.querySelector('[name="featured"]')?.checked===true};if(!data.name||!Number.isFinite(data.price)||data.price<0){alert('Confira nome e preço.');return}const btn=form.querySelector('button[type="submit"],.primary');if(btn){btn.disabled=true;btn.textContent='Criando produto e estoque…'}const{data:product,error:pe}=await sb.from('products').insert(data).select('id').single();if(pe){if(btn){btn.disabled=false;btn.textContent='Salvar produto'}alert(pe.message);return}const{error:ae}=await sb.from('product_accounts').insert(accounts.map(a=>({product_id:product.id,login:a.login,password:a.password,extra:null})));if(ae){await sb.from('products').delete().eq('id',product.id);if(btn){btn.disabled=false;btn.textContent='Salvar produto'}alert(`Não foi possível salvar o estoque: ${ae.message}`);return}location.reload()}
const observer=new MutationObserver(()=>{const form=document.querySelector('#product-form');if(form){addInventoryField(form);if(!form.dataset.inventoryBound){form.dataset.inventoryBound='1';form.addEventListener('submit',e=>handleSubmit(e,form),true)}}});observer.observe(document.body,{childList:true,subtree:true});
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let last='';function enhance(){const stats=document.querySelector('.stats');if(!stats||document.querySelector('.rx-admin-analytics'))return;const rows=[...document.querySelectorAll('#product-rows tr')];const low=rows.filter(r=>Number(r.children?.[3]?.textContent||0)<=3).slice(0,8);const paid=[...document.querySelectorAll('.status')].filter(x=>/paid|completed|delivered/i.test(x.textContent)).length;const el=document.createElement('section');el.className='rx-admin-analytics';el.innerHTML='<div class="rx-admin-card"><h3>Operação rápida</h3><div class="rx-admin-list"><div class="rx-admin-item"><span>Pedidos com status concluído</span><b>'+paid+'</b></div><div class="rx-admin-item"><span>Produtos com estoque crítico</span><b>'+low.length+'</b></div><div class="rx-admin-item"><span>Atualização</span><span class="rx-admin-muted">tempo real nesta sessão</span></div></div></div><div class="rx-admin-card"><h3>Estoque crítico</h3><div class="rx-admin-list">'+(low.length?low.map(r=>'<div class="rx-admin-item"><span>'+esc(r.children[0]?.innerText||'Produto')+'</span><b>'+esc(r.children[3]?.textContent||'0')+'</b></div>').join(''):'<span class="rx-admin-muted">Nenhum item crítico visível.</span>')+'</div></div>';stats.after(el)}const obs=new MutationObserver(()=>{const s=document.querySelector('.admin-shell')?.innerText?.slice(0,80)||'';if(s!==last){last=s;setTimeout(enhance,20)}});obs.observe(document.documentElement,{subtree:true,childList:true});setTimeout(enhance,300);
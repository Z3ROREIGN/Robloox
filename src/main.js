import { createClient } from '@supabase/supabase-js';
import './style.css';
import { icons } from './icons.js';
const url=import.meta.env.VITE_SUPABASE_URL,key=import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY,sb=url&&key?createClient(url,key):null;
const S={user:null,profile:null,cats:[],products:[],q:'',cat:'',cart:JSON.parse(localStorage.getItem('rooblox_cart')||'[]')};
const logo='<img class="logo" src="/logo.svg" alt="Rooblox">';
const esc=s=>String(s??'').replace(/[&<>"']/g,x=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[x]));
const brl=v=>Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
const save=()=>localStorage.setItem('rooblox_cart',JSON.stringify(S.cart));
const icon=s=>icons[s]||icons.box;
const toast=(m,e=false)=>{const x=document.createElement('div');x.className='toast '+(e?'error':'');x.innerHTML=`${e?icons.alert:icons.check}<span>${esc(m)}</span>`;document.body.append(x);setTimeout(()=>x.remove(),2800)};
async function load(){if(sb){const {data:{session}}=await sb.auth.getSession();S.user=session?.user||null;if(S.user){const p=await sb.from('profiles').select('*').eq('id',S.user.id).maybeSingle();S.profile=p.data||null}const c=await sb.from('store_categories').select('*').eq('active',true).order('sort_order');S.cats=c.data||[];const p=await sb.from('products').select('id,name,description,price,image_url,featured,compare_at_price,badge,sort_order,category_slug,stock_quantity,delivery_type').eq('active',true).order('featured',{ascending:false}).order('sort_order');S.products=p.data||[]}render()}
function shell(body){return `<header class="site-header"><div class="nav"><a href="#/" class="brand">${logo}</a><nav><a href="#/">Início</a><a href="#/catalogo">Produtos</a><a href="#/ajuda">Ajuda</a></nav><div class="right"><label class="search">${icons.search}<input id="q" value="${esc(S.q)}" placeholder="Buscar produtos..." aria-label="Buscar produtos"></label><a class="cart" href="#/checkout">${icons.cart}<span>Carrinho</span><b>${S.cart.length}</b></a>${S.user?`<a href="#/conta" class="account">${icons.user}<span>${esc(S.profile?.display_name||S.profile?.username||'Minha conta')}</span></a>`:'<a href="#/login" class="primary small">Entrar</a>'}</div></div></header><main>${body}</main><footer><div class="footer-brand">${logo}<p>Marketplace independente de produtos para jogos, criado para uma experiência simples e elegante.</p></div><div><strong>Explorar</strong><a href="#/catalogo">Produtos</a><a href="#/ajuda">Central de ajuda</a><a href="#/checkout">Carrinho</a></div><div><strong>Informações</strong><a href="#/termos">Termos de Uso</a><a href="#/privacidade">Privacidade</a><a href="#/reembolso">Reembolso</a></div><small>© 2026 Rooblox. Todos os direitos reservados.</small></footer>`}
function cards(a){return a.length?a.map(p=>`<article class="card"><a class="cover" href="#/produto/${p.id}">${p.image_url?`<img src="${esc(p.image_url)}" alt="${esc(p.name)}">`:`<span class="cover-fallback">${icon(p.category_slug||'box')}</span>`}${p.badge?`<i>${esc(p.badge)}</i>`:''}<span class="cover-arrow">${icons.arrow}</span></a><div class="card-body"><small>${esc(p.category_slug||'Produto')}</small><h3>${esc(p.name)}</h3><div class="price">${brl(p.price)} ${p.compare_at_price?`<del>${brl(p.compare_at_price)}</del>`:''}</div><button data-add="${p.id}" class="add">${icons.cart}Adicionar ao carrinho</button></div></article>`).join(''):'<div class="empty"><div class="empty-icon">'+icons.search+'</div><h3>Nenhum produto encontrado</h3><p>Novos produtos aparecerão aqui quando forem cadastrados.</p></div>'}
function home(){return `<section class="hero"><div class="hero-copy"><span class="eyebrow">MARKETPLACE ROOBLOX</span><h1>Seu universo de jogos.<br><em>Do seu jeito.</em></h1><p>Uma experiência de compra elegante para encontrar itens dos seus jogos favoritos com rapidez, clareza e segurança.</p><div class="hero-actions"><a class="primary" href="#/catalogo">Explorar produtos ${icons.arrow}</a><a class="ghost" href="#/ajuda">Como funciona</a></div><div class="hero-mini"><span>${icons.shield}<b>Compra segura</b></span><span>${icons.zap}<b>Checkout rápido</b></span></div></div><div class="hero-art"><div class="hero-card"><div class="hero-orb orb-a"></div><div class="hero-orb orb-b"></div>${logo}<span>ITENS • JOGOS • ROBUX</span><div class="hero-float float-one">${icons.game}<b>Seus jogos</b></div><div class="hero-float float-two">${icons.star}<b>Favoritos</b></div></div></div></section><section class="section"><div class="section-head"><div><span class="eyebrow dark">EXPLORE</span><h2>Categorias</h2><p>Escolha um universo e encontre o que procura.</p></div><a href="#/catalogo">Ver catálogo ${icons.arrow}</a></div><div class="cats">${S.cats.length?S.cats.map(c=>`<button data-cat="${esc(c.slug)}"><span class="cat-icon">${icon(c.slug)}</span><span class="cat-copy"><strong>${esc(c.name)}</strong><small>${esc(c.description||'Itens e produtos')}</small></span><span class="cat-arrow">${icons.arrow}</span></button>`).join(''):'<div class="empty"><h3>Categorias em preparação</h3><p>Conecte o banco para exibir as categorias.</p></div>'}</div></section><section class="section section-products"><div class="section-head"><div><span class="eyebrow dark">SELEÇÃO</span><h2>Em destaque</h2><p>Produtos selecionados para você.</p></div><a href="#/catalogo">Ver todos ${icons.arrow}</a></div><div class="grid">${cards(S.products.filter(p=>p.featured).slice(0,8))}</div></section><section class="trust"><div>${icons.shield}<b>Compra segura</b><span>Pagamento e pedidos organizados em um só lugar.</span></div><div>${icons.pix}<b>PIX</b><span>Pagamento processado pela MisticPay.</span></div><div>${icons.support}<b>Suporte</b><span>Conte com a Central de Ajuda quando precisar.</span></div></section>`}
function catalog(){let a=S.products.filter(p=>(!S.cat||p.category_slug===S.cat)&&(!S.q||(`${p.name} ${p.description||''}`).toLowerCase().includes(S.q.toLowerCase())));return `<section class="page"><div class="page-head"><div><span class="eyebrow dark">CATÁLOGO</span><h1>Produtos</h1><p>Encontre itens para seus jogos favoritos.</p></div><span class="result-count">${a.length} produto(s)</span></div><div class="filters"><button data-cat="" class="${!S.cat?'active':''}">${icons.grid}Todos</button>${S.cats.map(c=>`<button data-cat="${esc(c.slug)}" class="${S.cat===c.slug?'active':''}">${icon(c.slug)}${esc(c.name)}</button>`).join('')}</div><div class="grid">${cards(a)}</div></section>`}
function product(id){let p=S.products.find(x=>x.id===id);return `<section class="page">${p?`<a class="back" href="#/catalogo">${icons.arrowLeft} Voltar ao catálogo</a><div class="product"><div class="product-image">${p.image_url?`<img src="${esc(p.image_url)}" alt="${esc(p.name)}">`:`<span>${icon(p.category_slug||'box')}</span>`}</div><div class="product-info"><span class="eyebrow dark">${esc(p.category_slug||'PRODUTO')}</span><h1>${esc(p.name)}</h1><p class="lead">${esc(p.description||'Produto digital para seu jogo favorito.')}</p><div class="big-price">${brl(p.price)}</div><div class="stock">${p.stock_quantity>0?`${icons.check} ${p.stock_quantity} unidade(s) disponíveis`:`${icons.info} Disponibilidade sob consulta`}</div><button data-add="${p.id}" class="primary wide">${icons.cart} Adicionar ao carrinho</button><div class="purchase-note">${icons.shield} Compra protegida • Entrega conforme o produto</div></div></div>`:'<div class="empty"><h3>Produto não encontrado</h3><a class="primary" href="#/catalogo">Voltar ao catálogo</a></div>'}</section>`}
function auth(reg){return `<section class="auth"><div class="auth-card">${logo}<span class="eyebrow dark">${reg?'CRIAR CONTA':'ENTRAR'}</span><h1>${reg?'Crie sua conta':'Bem-vindo de volta'}</h1><p class="auth-sub">${reg?'Tenha seus pedidos e compras organizados em um só lugar.':'Acesse sua conta para continuar.'}</p><form id="auth">${reg?'<label>Nome de usuário<input name="username" minlength="3" maxlength="24" required placeholder="Seu nome"></label>':''}<label>E-mail<input name="email" type="email" required placeholder="voce@email.com"></label><label>Senha<input name="password" type="password" minlength="6" required placeholder="••••••••"></label>${reg?'<label class="check"><input type="checkbox" required> Aceito os <a href="#/termos">Termos de Uso</a> e a Política de Privacidade.</label>':''}<button class="primary wide">${reg?icons.user+' Criar conta':icons.arrow+' Entrar'}</button></form><p>${reg?'Já possui conta?':'Ainda não possui conta?'} <a href="#/${reg?'login':'cadastro'}">${reg?'Entrar':'Criar conta'}</a></p></div></section>`}
function checkout(){let total=S.cart.reduce((n,x)=>n+x.price*x.quantity,0);return `<section class="page"><div class="page-head"><div><span class="eyebrow dark">CHECKOUT</span><h1>Finalizar compra</h1><p>Revise seu pedido antes de pagar.</p></div></div><div class="checkout"><div class="panel"><div class="panel-title"><span>${icons.cart}</span><div><h3>Seu carrinho</h3><p>${S.cart.length} item(ns)</p></div></div>${S.cart.length?S.cart.map(x=>`<div class="line"><span>${esc(x.name)} <small>× ${x.quantity}</small></span><b>${brl(x.price*x.quantity)}</b></div>`).join(''):'<div class="empty compact"><h3>Seu carrinho está vazio</h3><a class="primary" href="#/catalogo">Explorar produtos</a></div>'}</div><div class="panel payment-panel"><div class="panel-title"><span>${icons.pix}</span><div><h3>Pagamento</h3><p>PIX instantâneo</p></div></div><div class="total"><span>Total</span><b>${brl(total)}</b></div><button id="pay" class="primary wide" ${!S.user||!S.cart.length?'disabled':''}>${icons.pix} Pagar com PIX</button>${!S.user?'<div class="checkout-alert">Entre na sua conta para continuar.</div>':''}<div id="payment"></div></div></div></section>`}
function account(){if(!S.user)return auth(false);return `<section class="page"><div class="profile"><div class="avatar">${esc((S.profile?.display_name||S.profile?.username||S.user.email||'R')[0].toUpperCase())}</div><div><span class="eyebrow dark">MINHA CONTA</span><h1>${esc(S.profile?.display_name||S.profile?.username||'Minha conta')}</h1><p>${esc(S.user.email)}</p></div></div><div class="account-grid"><a class="panel" href="#/pedidos"><span class="panel-icon">${icons.orders}</span><b>Meus pedidos</b><span>Acompanhe compras e entregas.</span></a><a class="panel" href="#/catalogo"><span class="panel-icon">${icons.game}</span><b>Continuar comprando</b><span>Voltar ao catálogo.</span></a><a class="panel" href="#/ajuda"><span class="panel-icon">${icons.support}</span><b>Central de ajuda</b><span>Encontre respostas e suporte.</span></a><button id="logout" class="panel"><span class="panel-icon">${icons.logout}</span><b>Sair</b><span>Encerrar sessão.</span></button></div></section>`}
function page(r){let t={termos:'Termos de Uso',privacidade:'Política de Privacidade',reembolso:'Política de Reembolso'}[r];return `<section class="page document"><div class="document-head"><span class="eyebrow dark">ROOBLOX • INFORMAÇÕES</span><h1>${t}</h1><p>Informações importantes sobre o uso da plataforma.</p></div><div class="document-grid"><aside class="panel"><b>Documentos</b><a href="#/termos">Termos de Uso</a><a href="#/privacidade">Privacidade</a><a href="#/reembolso">Reembolso</a><a href="#/ajuda">Central de Ajuda</a></aside><article class="panel document-copy"><span>ATUALIZADO EM 2026</span><h2>${t}</h2><p>Este documento apresenta as regras e informações aplicáveis ao uso do Rooblox. O conteúdo poderá ser atualizado para refletir mudanças na plataforma, nos serviços ou na legislação aplicável.</p><h3>1. Informações gerais</h3><p>Use a plataforma de forma responsável e forneça informações corretas quando necessário para concluir uma compra ou receber suporte.</p><h3>2. Compras e pedidos</h3><p>Os detalhes de cada compra, pagamento e entrega ficam vinculados ao pedido correspondente. Em caso de problema, utilize a Central de Ajuda.</p><h3>3. Atendimento</h3><p>Se precisar de assistência, informe o número do pedido e descreva o problema de forma objetiva para facilitar o atendimento.</p></article></div></section>`}
function help(){return `<section class="page"><div class="page-head"><div><span class="eyebrow dark">SUPORTE</span><h1>Central de Ajuda</h1><p>Respostas rápidas e caminhos para resolver seu problema.</p></div></div><div class="help"><a class="panel" href="#/checkout"><span class="help-icon">${icons.pix}</span><b>Pagamento</b><span>Entenda o checkout e o PIX.</span></a><a class="panel" href="#/pedidos"><span class="help-icon">${icons.orders}</span><b>Pedidos</b><span>Acompanhe compras e entregas.</span></a><a class="panel" href="#/catalogo"><span class="help-icon">${icons.box}</span><b>Produtos</b><span>Veja categorias e disponibilidade.</span></a><a class="panel" href="#/conta"><span class="help-icon">${icons.user}</span><b>Minha conta</b><span>Gerencie seu acesso e pedidos.</span></a></div><div class="support-banner panel"><div><span class="eyebrow dark">PRECISA DE AJUDA?</span><h2>Estamos aqui para ajudar.</h2></div><a class="primary" href="mailto:suporte@rooblox.com">Falar com suporte ${icons.arrow}</a></div></section>`}
function orders(){return S.user?`<section class="page"><div class="page-head"><div><span class="eyebrow dark">HISTÓRICO</span><h1>Meus pedidos</h1><p>Acompanhe seus pedidos e respectivos status.</p></div></div><div id="orders" class="empty">Carregando pedidos...</div></section>`:auth(false)}
function admin(){return `<section class="page"><div class="page-head"><div><span class="eyebrow dark">ADMINISTRAÇÃO</span><h1>Painel Rooblox</h1><p>Visão geral da operação.</p></div></div><div class="admin-grid"><div class="panel"><span class="panel-icon">${icons.box}</span><b>${S.products.length}</b><span>Produtos ativos</span></div><div class="panel"><span class="panel-icon">${icons.grid}</span><b>${S.cats.length}</b><span>Categorias</span></div><div class="panel"><span class="panel-icon">${icons.cart}</span><b>${S.cart.length}</b><span>Itens no carrinho</span></div></div><div class="panel admin-note"><h3>Gestão</h3><p>Produtos, categorias, pedidos, pagamentos, suporte, usuários, cargos e auditoria devem ser administrados no backend com as políticas de segurança do Supabase.</p></div></section>`}
function route(){let [r,id]=location.hash.slice(2).split('/');return r==='catalogo'?catalog():r==='produto'?product(id):r==='login'?auth(false):r==='cadastro'?auth(true):r==='checkout'?checkout():r==='conta'?account():r==='pedidos'?orders():r==='ajuda'?help():r==='admin'?admin():['termos','privacidade','reembolso'].includes(r)?page(r):home()}
function render(){document.querySelector('#app').innerHTML=shell(route());bind()}
function bind(){document.querySelector('#q')?.addEventListener('input',e=>{S.q=e.target.value;render()});document.querySelectorAll('[data-cat]').forEach(b=>b.onclick=()=>{S.cat=b.dataset.cat;location.hash='#/catalogo';render()});document.querySelectorAll('[data-add]').forEach(b=>b.onclick=()=>{let p=S.products.find(x=>x.id===b.dataset.add);if(!p)return;let o=S.cart.find(x=>x.id===p.id);o?o.quantity++:S.cart.push({id:p.id,name:p.name,price:Number(p.price),quantity:1});save();toast('Produto adicionado ao carrinho');render()});document.querySelector('#auth')?.addEventListener('submit',submit);document.querySelector('#logout')?.addEventListener('click',async()=>{if(sb)await sb.auth.signOut();S.user=null;render()});document.querySelector('#pay')?.addEventListener('click',()=>toast('Pagamento PIX será ativado após configurar a Edge Function da MisticPay.'));if(location.hash==='#/pedidos')loadOrders()}
async function submit(e){e.preventDefault();if(!sb)return toast('Configure as variáveis do Supabase no ambiente.',true);let f=new FormData(e.currentTarget),reg=location.hash.includes('cadastro'),r=reg?await sb.auth.signUp({email:f.get('email'),password:f.get('password'),options:{data:{username:f.get('username')}}}):await sb.auth.signInWithPassword({email:f.get('email'),password:f.get('password')});if(r.error)return toast(r.error.message,true);await load();location.hash='#/'}
async function loadOrders(){if(!sb||!S.user)return;let {data,error}=await sb.from('orders').select('id,amount,status,created_at').eq('user_id',S.user.id).order('created_at',{ascending:false}),el=document.querySelector('#orders');if(!el)return;if(error){el.innerHTML='<div class="empty"><h3>Não foi possível carregar seus pedidos</h3><p>Tente novamente em alguns instantes.</p></div>';return}el.innerHTML=data?.length?`<div class="order-list">${data.map(o=>`<div class="order panel"><span>${icons.orders}</span><b>#${o.id.slice(0,8)}</b><span>${brl(o.amount)}</span><strong>${esc(o.status)}</strong></div>`).join('')}</div>`:'<div class="empty"><h3>Você ainda não possui pedidos</h3><a class="primary" href="#/catalogo">Explorar produtos</a></div>'}
window.addEventListener('hashchange',render);load();
